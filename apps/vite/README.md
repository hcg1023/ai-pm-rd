# AI 对话助手 - 使用指南

一个现代化、功能丰富的 AI 聊天应用，使用 React + TypeScript + Tailwind CSS 构建。

## ✨ 特性

- 🎨 **现代化 UI 设计** - 采用 Tailwind CSS 构建，拥有渐变色彩和流畅动画
- 💬 **流式响应** - 实时显示 AI 回复，提供更好的交互体验
- 📚 **多轮对话** - 支持上下文记忆，实现真正的智能对话
- 🎯 **快捷建议** - 首页提供预设问题，快速开始对话
- 📥 **导出功能** - 将对话记录导出为文本文件
- 🗑️ **清除历史** - 一键清除所有对话记录
- 🌓 **响应式设计** - 适配各种屏幕尺寸
- ⌨️ **快捷键支持** - Enter 发送，Shift+Enter 换行

## 🚀 快速开始

### 前置要求

- Node.js 20.19+ 或 22.12+
- pnpm

### 安装依赖

```bash
pnpm install
```

### 配置环境变量

确保后端服务已配置好 OpenAI API 密钥：

```bash
cd apps/nestjs
cp .env.example .env
# 编辑 .env 文件，填入你的 OpenAI API 密钥
```

### 启动开发服务器

**启动前端（Vite）：**
```bash
cd apps/vite
pnpm dev
```

前端将在 http://localhost:5173 运行

**启动后端（NestJS）：**
```bash
cd apps/nestjs
pnpm start:dev
```

后端将在 http://localhost:3000 运行

### 构建生产版本

```bash
cd apps/vite
pnpm build
```

构建产物将生成在 `dist` 目录中。

## 🎨 UI 特性

### 设计亮点

1. **渐变顶部栏** - 蓝紫色渐变，视觉效果出色
2. **圆形头像** - 用户和 AI 消息使用不同颜色区分
3. **阴影效果** - 消息气泡带有阴影，增强层次感
4. **加载动画** - 三点跳动动画，显示 AI 正在思考
5. **光标闪烁** - 流式响应时的打字效果
6. **悬停效果** - 按钮和卡片的悬停动画

### 交互优化

- **自动滚动** - 新消息自动滚动到底部
- **输入自适应** - 文本框高度自动调整
- **按钮状态** - 加载时禁用发送按钮
- **快捷建议** - 点击预设问题自动填充并发送
- **对话统计** - 实时显示对话轮数

## 📦 技术栈

### 前端

- **React 19.2** - UI 框架
- **TypeScript** - 类型安全
- **Vite 7.2** - 构建工具
- **Tailwind CSS 3.4** - 样式框架
- **PostCSS** - CSS 处理

### 后端 API

- **NestJS** - 后端框架
- **OpenAI SDK** - AI 模型集成
- **Server-Sent Events** - 流式响应

## 🔧 项目结构

```
apps/vite/
├── src/
│   ├── components/
│   │   ├── ChatComponent.tsx      # 原始聊天组件
│   │   └── ModernChat.tsx         # 现代化聊天组件
│   ├── App.tsx                    # 主应用组件
│   ├── index.css                  # 全局样式（Tailwind）
│   └── main.tsx                   # 应用入口
├── tailwind.config.js             # Tailwind 配置
├── vite.config.ts                 # Vite 配置
└── package.json                   # 依赖配置
```

## 💡 使用技巧

1. **多轮对话** - AI 会记住之前的对话内容，支持连续对话
2. **快速开始** - 点击首页的建议卡片快速开始
3. **导出记录** - 重要对话可以导出保存
4. **清除历史** - 开始新话题前清除旧对话
5. **键盘快捷键** - 使用 Enter 快速发送消息

## 🎯 未来改进

- [ ] 支持 Markdown 渲染
- [ ] 代码高亮显示
- [ ] 多语言支持
- [ ] 主题切换（深色/浅色模式）
- [ ] 对话历史持久化
- [ ] 搜索历史记录
- [ ] 语音输入
- [ ] 图片上传

## 🐛 故障排除

### 构建失败

如果遇到构建错误，确保：
- Node.js 版本 >= 20.19
- 清除 node_modules 并重新安装：`rm -rf node_modules && pnpm install`

### API 连接失败

检查：
- 后端服务是否运行在 http://localhost:3000
- Vite 代理配置是否正确（vite.config.ts）
- OpenAI API 密钥是否配置正确

### 样式问题

如果 Tailwind CSS 样式不生效：
- 确认已安装 `@tailwindcss/postcss` 和 `tailwindcss@3`
- 检查 postcss.config.js 配置
- 确认 index.css 中包含 Tailwind 指令

## 📄 License

MIT

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**享受与 AI 的智能对话！** 🤖✨
